# Get specials called in current source file

Get specials called in current source file

## Usage

``` r
get_special_calls(xml)
```

## Arguments

- xml:

  An XML node list

## Value

A list of `xml_nodes` and `text`.
