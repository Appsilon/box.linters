# Check if functions being attached exist and are being exported by the library/package

Check if functions being attached exist and are being exported by the
library/package

## Usage

``` r
check_attached_pkg_funs(xml, xpath)
```

## Value

An XML node list

## See also

[`get_packages_exports()`](https://appsilon.github.io/box.linters/reference/get_packages_exports.md)
