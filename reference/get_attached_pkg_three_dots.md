# Get all functions exported from packages by ...

Get all functions exported from packages by ...

## Usage

``` r
get_attached_pkg_three_dots(xml)
```

## Arguments

- xml:

  An XML node list

## Value

`xml` list of `xml_nodes`, `nested` list of `package: function`, `text`
a list of function names.
