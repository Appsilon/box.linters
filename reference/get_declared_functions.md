# Get locally declared/defined functions

Get locally declared/defined functions

## Usage

``` r
get_declared_functions(xml)
```

## Arguments

- xml:

  An XML node list.

## Value

A list of `xml_nodes` and `text`.
