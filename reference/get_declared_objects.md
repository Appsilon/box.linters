# Get locally declared/defined data objects

Get locally declared/defined data objects

## Usage

``` r
get_declared_objects(xml)
```

## Arguments

- xml:

  An XML node list

## Value

A list of `xml_nodes` and `text`.
