# Get all functions exported from modules by ...

Get all functions exported from modules by ...

## Usage

``` r
get_attached_mod_three_dots(xml)
```

## Arguments

- xml:

  An XML node list

## Value

`xml` list of `xml_nodes`, `nested` list of `module: function`, `text`
list of function names.
